server:
  port�� 8761

eureka:
  instance:
    hostname: localhost
  client:
    registerWithEureka: false
    fetchRegister: false
    serviceUrl:
      defalutZone: http://${eureka.instance.hostname}:${server.port}/eureka/
spring:
  application:
    name: eurka-server